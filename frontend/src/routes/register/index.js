import { h, Component } from 'preact';

export default class Register extends Component {
    render() {
        return (
            <form id="register-form">
                <h1>Register Form</h1>
            </form>
        )
    }
}
